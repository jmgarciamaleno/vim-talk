09 Folding (:help folding)

set foldenable
set foldmethod=syntax (indent, diff)
set foldcolumn=#

zo (open)
zO (open recursively)
zc (close)
zC (close recursively)

zr (reduce folding)
zR (open all)

zm (more folding)
zM (close all)
